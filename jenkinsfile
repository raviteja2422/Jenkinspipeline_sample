properties([[$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/raviteja2422/Jenkinspipeline_sample.git/'], pipelineTriggers([githubPush()])])
properties([[$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/raviteja2422/Jenkinspipeline_sample.git/'], pipelineTriggers([upstream('branch1')])])
pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps { 
                sh 'ls'
            }
        }
        stage('Test'){
            steps {
                sh 'pwd'
            }
        }
        stage('Deploy') {
            steps {
                sh 'pwd'
            }
        }
    }
}
